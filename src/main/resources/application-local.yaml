spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/zcommerce
    username: postgres
    password: mysecretpassword
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        default_schema: core
